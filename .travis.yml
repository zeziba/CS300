language: ruby
cache: bundler
bundler_args: --without production

rvm:
 - 2.7.0
 
before_install:
 - gem install bundler:2.2.11

script:
 - bundle exec rspec
 
deploy:
 provider: heroku
 api_key: 09ff79be-a433-4034-afd8-125220944709
 app: fast-waters-11904
 run: "rake db:migrate"
 on:
  all_branches: true