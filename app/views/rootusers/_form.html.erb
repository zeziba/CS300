<%= form_with(model: rootuser, local: true) do |form| %>
  <% if rootuser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rootuser.errors.count, "error") %> prohibited this rootuser from being saved:</h2>

      <ul>
      <% rootuser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
