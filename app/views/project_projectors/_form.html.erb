<%= form_with(model: project_projector, local: true) do |form| %>
  <% if project_projector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_projector.errors.count, "error") %> prohibited this project_projector from being saved:</h2>

      <ul>
      <% project_projector.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
